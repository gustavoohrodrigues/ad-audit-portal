# Banco (async)
sqlalchemy[asyncio]==2.0.51
psycopg[binary,pool]==3.2.3

# HTTP para conectores SIEM
httpx==0.28.1

# Opcionais (WinRM) — descomente se usar EVENT_COLLECTOR_MODE=winrm
# pywinrm==0.5.0
# xmltodict==0.14.2
